# Bright Data live run — Zerodha Fund House — 2026-08-20

- Collector: `c_mt0gzmiq1zdx7m835o`
- Source: `https://careers.zerodhafundhouse.com/jobs`
- Result artifact: `artifacts/zfh-run.json` (ignored local artifact; not committed)
- Returned rows: 13
- Ingestion status: success
- Local database: `data/applysignal.db` (the refreshed live snapshot is now tracked for the hosted deployment)
- Data mode: `live`

## Observed schema

The live collector returned `source_job_id`, `title`, `department`, `location`, `employment_type`, `posted_date_text`, HTML description, `job_detail_url`, `application_url`, and `product_page_url`.

The normalizer preserves the raw date text in provenance, resolves the observed UTC timestamp to an exact date where parseable, and treats `job_detail_url` as the listing URL. It does not infer compensation, closing dates, application fields, or vacancy identity from absent values.

## Verification

- Collector command exited successfully and wrote JSON output.
- JSON output contained 13 rows.
- Cardinality guard accepted the run because the configured minimum was 1.
- Ingestion created 13 observation IDs.
- Running the local server returned 13 `/api/summary` source-confidence entries and 13 analyses.
- The dashboard root served the ApplySignal UI.

## Application-form run

- Collector: `c_mt0h6zkc2od06t5olc`
- Target: public Senior Software Engineer - Backend Development application form
- Result artifact: `artifacts/zfh-backend-application-run.json` (ignored local artifact; not committed)
- Visible fields returned: 17
- Ingested fields: 17 onto observation `obs_bc42aa31716c3573`
- Required fields observed: 9
- The fields include resume, identity, current location, employment history, education, current/expected CTC, notice period, and relocation willingness.

With the listing and application observations combined, the live backend role is labeled `information asymmetry`: six listing disclosure categories versus 17 requested application fields, nine marked required. This label is an explainable comparison, not a claim that the employer or role is fraudulent.

## Remaining gaps

- The application collector currently covers one live role; expand only after the source and target preflight decisions are recorded.
- Candidate-level Marketplace/library lookup evidence remains required for the remaining proposed sources.

## Self-healing run

- Repair request: preserve existing listing fields and add `closing_date_text` when a public deadline is visible; otherwise return `null`.
- Heal artifact: `artifacts/zfh-heal.json` (ignored local artifact)
- Approval artifact: `artifacts/zfh-heal-approve.json` (ignored local artifact)
- Initial heal state: `awaiting_approval`
- Reviewed preview: preserved existing fields and returned `closing_date_text: null` for a public listing without a deadline.
- Approval state: `done`
- Healed rerun artifact: `artifacts/zfh-run-healed.json` (ignored local artifact)
- Healed rerun rows: 13
- Healed output included `closing_date_text` and preserved `job_detail_url`.

## Reproducible CLI path

The repository command `bun run run:collector` was exercised against the healed collector with the authenticated CLI and returned 13 rows, persisted a live run, and created 13 observation IDs. The adapter uses Bright Data CLI's supported `--pretty` JSON output and applies the minimum-row guard before ingestion.

## Palantir Lever fallback

- Collector: `c_mt0ivvftqptif51k9`
- Target: `https://jobs.lever.co/palantir`
- Run mode: realtime automatically handed off to Bright Data batch mode after the realtime page limit
- Result: 307 rows, all with successful extraction status in the returned dataset
- Repository ingestion: `bun run run:collector` persisted the run as `sourceId=palantir`, `dataMode=live`, with 307 observation IDs
- Fields observed: requisition ID, title, team/department, location, employment type, description, detail URL, and application URL

The source is treated as a fallback live collector, not as proof that every candidate source is scrapeable. The unresolved Workday/custom targets remain explicitly labeled in the source catalog.

## Razorpay Greenhouse board

- Collector: `c_mt0jo8sc1rqz4ef0pb`
- Target: `https://job-boards.greenhouse.io/razorpaysoftwareprivatelimited`
- Result: 26 successful public listing rows
- Repository ingestion: `bun run run:collector` persisted the run as `sourceId=razorpay`, `dataMode=live`, with 26 observation IDs
- Fields observed: requisition ID, title, location, description, detail URL, application URL, and extraction status

The earlier Razorpay branded-page attempt remains documented as a partial routing result; the direct public board is the live source used by ApplySignal.

## Visa Workday scoped detail

- Collector: `c_mt0k0jqb14b08r1uxz`
- Target: `https://visa.wd5.myworkdayjobs.com/en-US/Visa/job/Sr-Software-Engineer_REF082824W`
- Result: one successful public detail-page row for requisition `REF082824W`
- Repository ingestion: `bun run run:collector` persisted one `sourceId=visa`, `dataMode=live` observation
- Fields observed: requisition ID, title, location, time type, raw `Posted 30+ Days Ago` text, description, detail URL, and application URL

This is labeled `live_scoped` in the source catalog. It demonstrates a valid Workday detail observation, not full-board coverage; the original Visa board collector remains a failed-generation attempt.

## Cadence Workday scoped detail

- Collector: `c_mt0kbe2b2abejp81k2`
- Target: `https://cadence.wd1.myworkdayjobs.com/en-US/External_Careers/job/Lead-Software-Engineer---Partitioning-and-Optimization-Algorithms_R55371`
- Result: one successful public detail-page row for requisition `R55371`
- Repository ingestion: `bun run run:collector` persisted one `sourceId=cadence`, `dataMode=live` observation
- Fields observed: requisition ID, title, location, time type, raw posted-date text, application deadline, description, detail URL, and application URL
- Quality caveat: Bright Data returned repeated location/time/date strings; the normalized observation preserves that raw output rather than inventing a cleaned value

This is labeled `live_scoped`; the full Cadence board collector remains unresolved.

The same command was re-run from the main repository checkout after integration; the main checkout database received 13 live observations and the local server reported 8 catalog entries, 2 successful runs, and 15 observations.
